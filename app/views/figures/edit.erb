<h1>Update Figure</h1>

<h2>Edits for <%= @figure.name %></h2>

<form action="/figures/<%= @figure.id %>" method="post">
    <input id="hidden" type="hidden" name="_method" value="patch">
<label>Edit the Figure Name: </label>
<br>
<input type="text" name="figure[name]" value="<% @figure.name %>" id="figure_name">
<br>

<p>Select an existing landmark or title, or create a new one below: </p>

<h3> Edit Landmarks: </h3>
<% Landmark.all.each do |landmark| %>
    <input type="checkbox" name="figure[landmark_ids][]" id="<%= landmark.name %>" value="<%= landmark.id %>"
    <%='checked' if @figure.landmarks.include?(landmark) %>> <% #dont forget that extra ">" %>
    <%= landmark.name %> </input> <br>
    <% end %>

<br>

<h3> New Landmark: </h3>
<label>Landmark Name:</label>
<input type="text" name="landmark[name]" id="new_landmark">
<br><br>

<h3> Edit Titles: </h3>
<% Title.all.each do |title| %>
    <input type="checkbox" name="figure[title_ids][]" id="<%= title.name %>" value="<%= title.id %>"
    <%='checked' if @figure.titles.include?(title) %>> <% #dont forget that extra ">" %>
    <%= title.name %> </input> <br>
    <% end %>

<br>

<h3> New Title: </h3>
<label>Title Name:</label>
<input type="text" name="title[name]" id="new_title">
<br><br>

<input type="submit" value="Edit Figure">

</form>